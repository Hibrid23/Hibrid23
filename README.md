    if row in range(row_max):
        stdio.writeln("True")
        
    elif col in range(col_max):
        stdio.writeln("True")
    
    else:
        stdio.writeln("ERROR: Sink in the wrong position")
